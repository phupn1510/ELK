input {

 udp {
    port => 25826
    buffer_size => 1452
    codec => collectd { }
    type => collectd
  }
}

output {

 if [type] == "collectd" {
   elasticsearch {
    hosts => "127.0.0.1:9200"
    index => "collectd-%{+YYYY.MM.dd}"
    }
}
}
